server:
  port: 8080

spring:
  main:
    web-application-type: reactive
  cloud:
    vault:
      enabled: true
      token: hvs.ARYJUiJ7xmUtK1vLcYgkb5HP
      host: localhost
      port: 8200
      scheme: http

management:
  endpoints:
    web:
      exposure:
        include: "*"
